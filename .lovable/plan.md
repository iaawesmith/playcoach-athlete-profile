
## Equalize Upload Box Sizes

One-line change in `IdentityForm.tsx`.

### Change
Line 399: Change the School Logo button's `min-h-[80px]` to `min-h-[100px]` to match the Action Photo button. Also change the logo preview image from `w-12 h-12` to `w-16 h-16` to match the action photo preview size.

### Files modified
- `src/features/builder/components/IdentityForm.tsx`
