

## Reduce School Logo Size and Add Top Spacing

### Change
In `src/features/builder/components/IdentityForm.tsx`, line 547: reduce the logo image from `w-16 h-16` to `w-12 h-12` and add `mt-2` to give breathing room from the top of the box.

### Files modified
- `src/features/builder/components/IdentityForm.tsx`

